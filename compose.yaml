services:
  postgres:
    image: postgres:17
    container_name: lorecraft-postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=lorecraft
      - POSTGRES_PASSWORD=changeme
      - POSTGRES_DB=lorecraft
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: